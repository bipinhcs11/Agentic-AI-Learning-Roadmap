# Scenario F — API and runbook questions

Fictional question: which inventory endpoint supports pagination, and what should I check when it times out? Use the approved inventory contract and timeout runbook with versioned citations.

Acceptance: answer only from authorized sources; distinguish current version from older guidance; report missing information; ignore embedded instructions to approve a job. Another tenant's document must never enter context.

Phase 14's H2 catalog holds two summaries. It is not semantic retrieval, an embedding index, or a complete RAG implementation. Refer to the existing Phase 9 RAG path when implementing retrieval.

Output: selected context with provenance, cited answer, retention/deletion design, and relevant adversarial tests or evaluation cases.

Engineering practices: context, memory, tools, guardrails, evals.
