# ═══════════════════════════════════════════════════════════════
# Agentic AI Learning Roadmap — requirements.txt
# Mac Mini M4  |  Updated: May 2026
# Install: pip install -r requirements.txt --break-system-packages
# ═══════════════════════════════════════════════════════════════

# ── Phase 1 — Core AI Stack ─────────────────────────────────
openai>=1.0.0
langchain>=0.1.0
langchain-community>=0.0.1
fastapi>=0.100.0
uvicorn>=0.23.0

# ── Phase 2 — RAG Stack ─────────────────────────────────────
pypdf>=3.0.0                  # read and parse PDF files
langchain-text-splitters>=0.0.1  # chunk documents
# NOTE: No chromadb or sentence-transformers — use Ollama nomic-embed-text
# NOTE: No PyTorch — causes OOM on 27b model. Use numpy for similarity.
numpy>=1.24.0                 # cosine similarity (RAM-safe alternative)

# ── Phase 3 — Full Agentic Stack ────────────────────────────
requests>=2.28.0              # HTTP requests for web scraping + Ollama API
beautifulsoup4>=4.12.0        # HTML parsing for web scraping agent
python-dotenv>=1.0.0          # load .env files for API keys
httpx>=0.24.0                 # async HTTP (used by FastAPI internally)
pydantic>=2.0.0               # data validation (already with FastAPI)
rich>=13.0.0                  # pretty terminal output for model manager

# ── Phase 4 — Agent Framework ───────────────────────────────
streamlit>=1.28.0             # web UI for chat interface (Project 04)
sqlalchemy>=2.0.0             # database ORM (for memory/storage projects)
tqdm>=4.65.0                  # progress bars for model downloads
typer>=0.9.0                  # CLI tool builder

# ── Optional / Cloud Deployment (install when ready) ────────
# modal                       # cloud deployment (needs modal.com account)
# langsmith                   # LangChain observability (needs API key)
# dspy-ai                     # prompt optimization framework
