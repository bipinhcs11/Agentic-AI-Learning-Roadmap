# ═══════════════════════════════════════════════════════
# Agentic AI Learning Roadmap — requirements.txt
# Mac Mini M4  |  Updated: May 3, 2026
# Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════

# ── Phase 1 — Core AI Stack ─────────────────────────────
openai>=1.0.0
langchain>=0.1.0
langchain-community>=0.0.1
fastapi>=0.100.0
uvicorn>=0.23.0

# ── Phase 2 — RAG Stack ─────────────────────────────────
chromadb>=0.4.0               # local vector database
sentence-transformers>=2.2.0  # convert text to embeddings
pypdf>=3.0.0                  # read and parse PDF files
langchain-text-splitters>=0.0.1  # chunk documents

# ── Phase 3 — Full Agentic Stack (install when ready) ───
# mem0ai                      # persistent agent memory
# scrapy                      # web scraping
# ragas                       # RAG evaluation
# langsmith                   # LangChain observability
